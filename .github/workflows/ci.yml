name: CPU contracts
on: [push, pull_request]
permissions:
  contents: read
jobs:
  contracts:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - name: Install CPU test dependencies
        run: |
          python -m pip install pytest==9.1.1
      - run: python scripts/run.py --cpu-only --output results/ci
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: cpu-contract-evidence
          path: results/ci
