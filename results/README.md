# Evidence

Run directories contain measured results and logs. CPU tests never certify a GPU backend. Source digests bind results to implementation and workload manifests. Clock settings are recorded, not changed.
